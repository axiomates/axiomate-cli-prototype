/**
 * 环境初始化模块
 * 在应用启动前配置必要的环境变量
 *
 * 此模块应在其他模块之前导入，以确保环境变量在依赖它们的库加载前就已设置
 */

// 强制启用颜色支持
// chalk 和 marked-terminal 等库依赖此环境变量来检测终端颜色支持
// 在某些环境下（如 Windows 或 CI），自动检测可能失败
process.env.FORCE_COLOR = "1";
